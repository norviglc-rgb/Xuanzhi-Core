# ARCHITECTURE_BASELINE.md

## 1. 文档目的
定义 Xuanzhi-Core 的基线架构、核心分层、主要组件职责与协同关系。
本文件回答：**系统是怎么分层的、每层做什么、组件之间怎么协作。**

---

## 2. 架构总述
Xuanzhi-Core 采用“治理核心 + 外部执行层 + 策略与契约硬化”的架构。

总体原则：
- 顶层治理，不吞执行
- 执行分层，不混职责
- 规则先显式，再硬化
- 集成通过边界，不通过混写

---

## 3. 分层结构

### Layer 0：根层常驻上下文层
作用：
- 提供项目身份、目标、记忆、工具总则、用户关系等高频全局信息

特征：
- 高频
- 稳定
- 短小
- 全局通用

### Layer 1：治理规范层
作用：
- 定义状态机、风险模型、术语、审批原则、字段规范、注册规范等

主要目录：
- governance/

### Layer 2：契约层
作用：
- 定义结构化输入输出契约
- 保证包结构、消息结构、事件结构、任务结构的一致性

主要目录：
- contracts/

### Layer 3：策略层
作用：
- 把可配置规则从 prose 中抽离为 machine-readable 配置

主要目录：
- policies/

### Layer 4：集成层
作用：
- 描述外部系统接入方式、职责边界、调用路径、数据交换原则

主要目录：
- integrations/

### Layer 5：执行资产层
作用：
- 承载具体 workflow、场景流程、自动化编排与应用协作说明

主要目录：
- workflows/
- skills/（如保留则需明确定义角色）

### Layer 6：记忆与沉淀层
作用：
- 保存时间切片记忆、阶段复盘、运行纪要、偏差记录

主要目录：
- memory/

---

## 4. 核心组件职责

### 4.1 Xuanzhi-Core
角色：
- 治理中枢
- 统一控制面
- 风险与状态约束中心
- 汇总与沉淀中心

职责：
- 接收任务
- 判断任务类别
- 决定执行路径
- 应用风险与审批规则
- 汇总结果
- 写入记忆
- 输出汇报

### 4.2 Claude Code
角色：
- 默认开发执行器

职责：
- 仓库内开发任务执行
- 文件修改、代码生成、实现推进
- 与治理要求对齐的执行输出

### 4.3 Coze Studio
角色：
- AI 应用中枢

职责：
- 承载 AI 应用
- 组织应用层 agent / flow
- 提供用户侧或业务侧应用入口

### 4.4 n8n
角色：
- 自动化与编排系统

职责：
- 定时触发
- 跨系统流程编排
- 自动化任务链路
- 事件驱动衔接

### 4.5 NocoBase
角色：
- 后台与人工审核平面

职责：
- 配置管理
- 表单承载
- 人工审核
- 审批与运营后台
- 结构化数据录入与维护

### 4.6 QMD
角色：
- 检索与记忆支撑层

职责：
- 提供检索能力
- 支撑记忆沉淀与回查
- 辅助上下文恢复

### 4.7 Higress
角色：
- 模型入口网关 / 路由层

职责：
- 统一模型访问入口
- 路由与代理
- 鉴权、网关策略、流量治理
- 为 AI 执行层提供统一模型接入面

### 4.8 GitLab CE
角色：
- 代码与项目边界系统

职责：
- 仓库管理
- MR / Issue / CI 边界
- 交付记录与协作边界

### 4.9 Docker
角色：
- 部署底座

职责：
- 服务承载
- 运行隔离
- 环境一致性保障

---

## 5. 典型协同路径

### 5.1 开发任务路径
用户/系统输入
-> Xuanzhi-Core 判断任务类型
-> 命中开发类任务
-> 路由到 Claude Code
-> 结果回收
-> 风险/质量检查
-> 记忆写入
-> 输出结果

### 5.2 自动化任务路径
定时器/事件源
-> n8n 触发流程
-> 调用相关服务
-> Xuanzhi-Core 负责治理约束与结果收敛
-> 写入记录或触发人工审核

### 5.3 人工审核路径
任务进入风险或例外状态
-> 根据规则要求进入审批
-> NocoBase 承载表单/审核动作
-> 审核结果回传
-> Xuanzhi-Core 更新状态与记录

### 5.4 AI 应用路径
业务场景进入 Coze Studio
-> 触发 AI 应用逻辑
-> 通过 Higress 访问模型层
-> 必要时调用检索/QMD
-> 结果回传至治理层做记录或汇报

---

## 6. 架构约束

### 6.1 不允许的架构倾向
1. 顶层治理层吞掉所有执行职责
2. 在根层堆积实现细节
3. 用文档重复表达已存在的策略配置
4. 没有契约就先扩展大量集成
5. 组件职责长期重叠却不清理

### 6.2 允许的架构演化
1. 新执行器接入
2. 新策略配置增加
3. 新 schema 增加
4. workflow 扩充
5. 控制器与校验器逐步落地
6. 部分 prose 规则转为可执行校验

---

## 7. 当前架构重点
当前阶段重点不是继续扩大架构图，而是：
1. 明确最小可运行闭环
2. 补齐关键空壳文档
3. 校正组件与目录映射
4. 做最小桥接或最小 validator
5. 让架构从“能解释”走向“能运行”

---

## 8. 维护要求
- 新增组件或替换组件时必须更新本文件
- 组件职责变化时必须同步修订
- 本文件不记录具体部署命令与实施手册