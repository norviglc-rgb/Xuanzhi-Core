# BOOTSTRAP.md

## 1. 文档目的
定义 Xuanzhi-Core 中 bootstrap 的角色、触发时机、最小内容与边界。

本文件用于回答：
- bootstrap 是什么
- 什么时候需要它
- 它与 `BOOT.md`、`PROJECT_BOOTSTRAP_SPEC.md` 的关系是什么
- 当前是否已经形成完整 bootstrap 机制

---

## 2. bootstrap 定义
bootstrap 是系统在新环境、新仓库、新阶段或新会话开始时，用于快速建立最小有效上下文的启动机制。

它的作用不是承载完整治理体系，而是：
- 建立最小起步认知
- 指向主事实源
- 避免启动阶段上下文混乱
- 让系统知道“先看什么、不要先看什么”

---

## 3. bootstrap 的使用场景
适用于以下情况：
1. 新仓库首次初始化
2. 新会话首次进入项目
3. 发生重大架构变化后的重新对齐
4. 新执行器接入后的起步同步
5. 从交接材料恢复项目状态时

---

## 4. bootstrap 的最小内容
bootstrap 至少应包含：

1. 项目是什么
2. 当前主事实源有哪些
3. 当前技术栈有哪些核心组件
4. 当前推荐先读哪些文件
5. 当前阶段处于哪里
6. 哪些材料不能当主事实源

---

## 5. 与其他文件的关系

### 5.1 与 `BOOT.md`
- `BOOT.md`：偏运行入口 / 启动指引
- `BOOTSTRAP.md`：偏 bootstrap 原则与最小启动结构

### 5.2 与 `governance/PROJECT_BOOTSTRAP_SPEC.md`
- `PROJECT_BOOTSTRAP_SPEC.md`：偏更细的规范层说明
- `BOOTSTRAP.md`：偏根层级概述与角色边界

---

## 6. 当前要求
当前 bootstrap 的重点不是做复杂流程，而是：
- 指向六类基线文档
- 指向 README
- 指明 `.claude/` 与 `*.bak.md` 不属于主事实源
- 指明当前技术栈已包含 Higress / Coze Studio / n8n / NocoBase

---

## 7. 非目标
bootstrap 不负责：
- 替代完整架构文档
- 承载详细 workflow 手册
- 记录所有历史上下文
- 取代治理、边界、变更控制、质量门禁文档

---

## 8. 当前状态说明
当前 bootstrap 机制处于：
- 已建立文档位
- 正在与正式基线体系对齐
- 尚未形成复杂自动化流程

因此本文件目前应作为**最小有效版本**使用。

---

## 9. 维护要求
- 若启动方式发生明显变化，应同步更新本文件
- 若主事实源变化，应优先更新本文件中的引用关系